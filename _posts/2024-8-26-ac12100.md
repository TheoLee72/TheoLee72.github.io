---
title: baekjoon 12100:2048 (Easy)
date: 2024-8-26 23:36:22 +0900
categories: [Ps]
tags: [cpp, c++, ps, deque]
math: true
---

[baekjoon 12100 2048 (Easy)](https://www.acmicpc.net/problem/12100)

# 12100번 2048 (Easy)

## 접근
2048 구현 문제였다.  deuque를 이용해서 좀 수월하게 구현했다.  

## 코드
{% raw %}
```cpp
//12100
#include <iostream>
#include <vector>
#include <deque>
#include <queue>
using namespace std;
vector<vector<int>> rightFunc(vector<vector<int>>& v){
  int n = v.size();
  vector<vector<int>> ret(n, vector<int>(n));
  for(int i = 0; i < n; i++){
    deque<int> dq;
    for(int j = 0; j < n; j++){
      if(v[i][j] != 0){
        dq.push_back(v[i][j]);
      }
    }
    int idx = n-1;
    while(!dq.empty()){
      int curr = dq.back();
      dq.pop_back();
      if(!dq.empty() && curr == dq.back()){
        dq.pop_back();
        ret[i][idx] = curr*2;
      }
      else{
        ret[i][idx] = curr;
      }
      idx--;
    }
  }
  return ret;
}
vector<vector<int>> leftFunc(vector<vector<int>>& v){
  int n = v.size();
  vector<vector<int>> ret(n, vector<int>(n));
  for(int i = 0; i < n; i++){
    deque<int> dq;
    for(int j = 0; j < n; j++){
      if(v[i][j] != 0){
        dq.push_back(v[i][j]);
      }
    }
    int idx = 0;
    while(!dq.empty()){
      int curr = dq.front();
      dq.pop_front();
      if(!dq.empty() && curr == dq.front()){
        dq.pop_front();
        ret[i][idx] = curr*2;
      }
      else{
        ret[i][idx] = curr;
      }
      idx++;
    }
  }
  return ret;
}
vector<vector<int>> upFunc(vector<vector<int>>& v){
  int n = v.size();
  vector<vector<int>> ret(n, vector<int>(n));
  for(int j = 0; j < n; j++){
    deque<int> dq;
    for(int i = 0; i < n; i++){
      if(v[i][j] != 0){
        dq.push_back(v[i][j]);
      }
    }
    int idx = 0;
    while(!dq.empty()){
      int curr = dq.front();
      dq.pop_front();
      if(!dq.empty() && curr == dq.front()){
        dq.pop_front();
        ret[idx][j] = curr*2;
      }
      else{
        ret[idx][j] = curr;
      }
      idx++;
    }
  }
  return ret;
}
vector<vector<int>> downFunc(vector<vector<int>>& v){
  int n = v.size();
  vector<vector<int>> ret(n, vector<int>(n));
  for(int j = 0; j < n; j++){
    deque<int> dq;
    for(int i = 0; i < n; i++){
      if(v[i][j] != 0){
        dq.push_back(v[i][j]);
      }
    }
    int idx = n-1;
    while(!dq.empty()){
      int curr = dq.back();
      dq.pop_back();
      if(!dq.empty() && curr == dq.back()){
        dq.pop_back();
        ret[idx][j] = curr*2;
      }
      else{
        ret[idx][j] = curr;
      }
      idx--;
    }
  }
  return ret;
}
int main(){
  // ios::sync_with_stdio(false);
  // cin.tie(0); cout.tie(0);
  int n; cin >> n; vector<vector<int>> v(n, vector<int>(n));
  for(int i = 0; i < n; i++){
    for(int j = 0; j < n; j++){
      cin >> v[i][j];
    }
  }
  queue<vector<vector<int>>> q;
  q.push(v);
  int a = 1;
  a += 1<<2; a+= 1<<4; a += 1<<6; a += 1<<8;
  for(int i = 0; i < a; i++){
    q.push(rightFunc(q.front()));
    q.push(leftFunc(q.front()));
    q.push(upFunc(q.front()));
    q.push(downFunc(q.front()));
    q.pop();
  }
  int ans = 0;
  if(q.size() == 1<<10){
    while(!q.empty()){
      vector<vector<int>> target = q.front();
      q.pop();
      for(int i = 0; i < n; i++){
        for(int j = 0; j < n; j++){
          ans = max(ans, target[i][j]);
        }
      }
    }
  }
  cout << ans;
  return 0;
}
```
{% endraw %}


